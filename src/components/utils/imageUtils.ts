// src/utils/imageUtils.ts

/**
 * Повертає URL зображення (усі фото завантажуються вже стиснутими в корінь бакету)
 */
export const getThumbnailUrl = (originalUrl: string): string => {
    return originalUrl || ''
}